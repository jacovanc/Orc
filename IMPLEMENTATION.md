# Orc implementation record

## Scope

Milestones 1–7: authenticated Laravel application, versioned workflow domain, transactional workflow engine, seeded proof/real-Development/independent-QA workflows, durable Laravel↔Amp orchestration, operational UI, tests, and production deployment. Project management, explicit Amp-project routing, and minimal agent-assisted pairing are implemented. The earlier automated feedback-publication milestone is superseded by manual GitHub feedback; remaining hardening and the workflow editor follow. See the [future roadmap](docs/future-roadmap.md).

## Work log

- 2026-09-07: Documented domain boundary, data model, seeded graph, engine invariants, and phase limitations before application implementation.
- 2026-09-07: Confirmed the Laravel Cloud API credential is present and valid without exposing its value. Installed Cloud CLI v0.5 locally; that version does not support the newer non-interactive environment-token flow, so authenticated Cloud setup uses the documented REST API.
- 2026-09-07: Inspected Amp plugins after loading the plugin skill. Workspace plugin access is active, but no Orc/Amp orchestration integration plugin is installed; real Amp integration remains blocked and out of scope for this phase.
- 2026-09-07: Implemented Laravel 12 and Breeze authentication, six-table workflow persistence, the immutable seeded graph, centralized transactional engine, authenticated HTTP actions, and the server-rendered UI.
- 2026-09-07: Added regression coverage for transitions, both loop paths, numbered attempts, idempotency, competing outcomes, database active-attempt protection, invalid/stale/cancelled completions, human actions, append-only events, URL validation, and ownership.
- 2026-09-07: Exercised Development → QA → Human Review → Done through the rendered UI with a real browser. Browser console/errors were empty. Visual inspection passed after removing an unnecessary timeline viewport cap.
- 2026-09-07: Published commit `81493c8bf81fc3b1a53f5e65eeb13a767e7863b1` to `jacovanc/Orc` on GitHub, provisioned the Orc application and production environment on Laravel Cloud, attached a private persistent MySQL database, and deployed successfully.
- 2026-09-07: Began Milestones 4–5 by inspecting the installed Amp Plugin API and durable webhook contract after loading the plugin and webhook skills. Documented the signed, claim-before-create, at-most-once launch protocol before implementation. GitHub access includes private repositories, but no issue was clearly designated as safe for integration comments; external proof remains pending an explicit repository/issue selection.
- 2026-09-07: Created the user-authorized public proof issue `jacovanc/Orc#1`, implemented durable queued launches and persistent callbacks, and added the restricted project-local Amp proof plugin. The model receives neither directional secret, the webhook capability, nor a GitHub token.
- 2026-09-07: Stored the existing GitHub credential only in the owner-readable, gitignored plugin runtime configuration. API reads returned HTTP 200 for the approved public proof issue and a private repository issue, proving the configured credential—not merely anonymous public access—has private read access. No private issue was modified and no spawned-agent private proof is claimed.
- 2026-09-07: Added integration UI states for dispatch, delivery, launch ambiguity/failure, real thread links, and GitHub report links. Simulation controls now appear only when Amp integration is disabled.
- 2026-09-07: Deployed integration commit `ff5732952ed511ffa2fced9d4fa00a7e008782cd` without destructive database changes. Provisioned one supervised Laravel Cloud database queue worker dedicated to `amp-launches` and configured the separate directional secrets with integration disabled until the durable Amp webhook is activated.
- 2026-09-07: Live proof remains pending because this already-running thread has no `load_plugin`/`reload_plugins` tool and standalone `amp plugins` execution is correctly denied `amp.createWebhook`. The user was asked to run `plugins: reload` in the same thread; no webhook capability was exposed or fabricated.
- 2026-09-07: Activated the durable controller webhook, enabled production integration, and verified repeated failed proof attempts failed closed: no guessed transition or duplicate Orb was created, and the safety net never made code changes. Diagnostics showed fresh-Orb custom agents cannot resolve project-local worker tool registrations, so the restricted worker was installed as User Plugin `orc-worker`; it stays inert unless Orc project-scoped configuration is present.
- 2026-09-07: Corrected project-scoped Amp values to exactly match production (including removing trailing newlines introduced by temporary data files). The credentials remained in owner-only files and secret stores and were never printed.
- 2026-09-07: Production `RUN-0015` proved the complete Milestones 4–5 path: Development fresh Orb/thread `T-01a07db9-0dc5-752c-a503-8be0cc5ce95d` read issue `jacovanc/Orc#1`, published its labelled report, and completed `success`; QA fresh Orb/thread `T-01a07db9-7853-759c-8e74-ebfdb89e0911` read the issue and Development report, published its own labelled report, and completed `pass`; Laravel transactionally entered waiting Human Review. Both Amp records report sandbox executors and distinct thread IDs.
- 2026-09-07: Investigated the independent GPT-5.6 Sol review `T-01a07dc9-901f-758c-9833-b5acaef9506b`. Initial hardening added fail-closed repository/operator allowlists, disabled production registration, nonce- and author-bound report markers, durable report attestation, full comment pagination, and a corrected Composer lock hash. Later user requirements superseded the review's restrictive-tool and copied-credential recommendations.
- 2026-09-07: Documented the Milestone 6 design before implementation in `docs/milestone-6-design.md`. The only open authorized Orc issue remains proof-only issue #1, so no suitable real coding target exists and no live coding verification is claimed.
- 2026-09-07: User requirements superseded the earlier restricted-agent/token design. Verified Amp's supported additive semantics (`extends: 'medium'` with `tools.add`), restored normal default tools for proof and Development agents, removed the worker's Orc-owned GitHub token dependency, and switched worker GitHub operations to native Orb `gh` authentication. Native issue/timeline pagination and a Git push dry run succeeded without Orc credential injection.
- 2026-09-07: Implemented workflow definition v2 for real Development `success|blocked`, proof-only QA `proof_complete`, blocked human retry, Human Review, and Done. Added encrypted per-launch capabilities so full-shell coding Orbs never receive broad callback signing secrets; Laravel enforces exact attempt/thread/mode/outcome/report/branch/PR bindings transactionally.
- 2026-09-07: Removed the positively identified Orc-created `ORC_GITHUB_TOKEN`, `ORC_LAUNCH_SIGNING_SECRET`, `ORC_CALLBACK_SIGNING_SECRET`, and `ORC_CALLBACK_URL` Amp project entries; `amp secrets list --project` now reports no project secrets. The trusted controller's mode-`0600` local file contains only `callbackUrl`, `launchSigningSecret`, and `callbackSigningSecret`. Native user `git`/`gh` authentication was not changed or inspected for secret values.
- 2026-09-07: Published the secretless native-auth User Plugin worker as commits `98c37b5` and `b6c3075`. The latter adds recovery when Laravel durably accepted `workflow_complete` but its response was lost. A plugin reload is required before a newly launched stage uses this worker revision.
- 2026-09-07: Hardened account provisioning so registration is source-default-disabled as well as disabled in production. The independent operator and repository allowlists remain mandatory whenever Amp integration is active; an end-to-end regression proves that even an explicitly enabled new registration cannot create a workflow, `AmpLaunch`, or queue job unless its email is separately allowlisted.
- 2026-09-08: Created authorized documentation-only acceptance issue `jacovanc/Orc#2` and started real workflow v2 as `RUN-0016`. The durable v1 webhook retained its older proof-only loaded handler despite plugin reload, launching thread `T-01a08085-8d04-74d2-aed8-a14b53c4126d`; native issue reads failed, no repository mutation or GitHub report occurred, and the guarded safety path failed the attempt/workflow. The failed run remains in the audit trail. Rotated the controller registration to versioned key `orc-stage-launch-v2` before retrying rather than concealing or mutating the attempt.
- 2026-09-08: Deployed controller rotation commit `48e01ef560bcec3b4c950f34111354e86bc82849` as Laravel Cloud deployment `depl-a2b23a8d-4570-4d43-98eb-8afa1d46d11b`, securely replaced only `AMP_LAUNCH_WEBHOOK_URL`, and confirmed production registration remained disabled and integration enabled.
- 2026-09-08: Production `RUN-0017` live-proved Milestone 6 on issue `jacovanc/Orc#2`. Development thread `T-01a08098-ff9f-7367-af7a-99f1a0091ce4` rebased its documentation-only commit onto GitHub main, pushed `orc/stage-19-attempt-1` at `12c6a25fe389c328d44d4127e8f5fb0d23ff276c`, opened unmerged PR #3, bound it, published a substantive success report, and completed `success`. Distinct QA thread `T-01a0809e-cbc1-767a-8ca5-2396c88216c5` made no repository changes, published an explicitly proof-only report, and completed `proof_complete`. Laravel is waiting at Human Review; no approval or merge occurred.
- 2026-09-08: The live Development Orb initially inherited an Amp-hosted `origin` and obsolete empty base, pushed its first local documentation commit only to that Amp branch, then safely recovered by fetching GitHub main, rebasing, and pushing the corrected branch explicitly to GitHub. Updated controller guidance to require target-repository/default-branch verification before editing and prohibit pushes to unrelated remotes. The audit evidence and extra Amp-hosted branch remain intact.
- 2026-09-08: Deployed the tracked live-acceptance record and improved controller guidance in Laravel Cloud deployment `depl-a2b241fa-7d9b-42ba-bd80-6004991a185c` from commit `8ba8c4b36baf742c371b36e55f9fe20df443d994`. Versioned the durable controller key to v3 so the verified-target-remote behavior cannot silently retain the already loaded v2 prompt.
- 2026-09-08: Corrected `RUN-0017` ownership after the acceptance bootstrap selected a different allowlisted operator than the account the user actually used. Added the user-specified existing account to the operator allowlist without removing prior entries, deployed a guard that rechecks both allowlists before any human action can launch another agent, transactionally reassigned only `RUN-0017`, and recorded append-only `workflow.owner_reassigned` audit metadata using user IDs rather than email addresses. The user had meanwhile merged PR #3 directly on GitHub; Orc still required its separate Human Review action and did not infer approval from that external merge.
- 2026-09-08: The user completed the separate Human Review approval in production. Verified `RUN-0017` is `completed` at terminal stage `done`, has a completion timestamp, exactly one recorded approval event, terminal attempt 4, and no active attempt.
- 2026-09-08: Documented the Milestone 7 design before implementation. Added immutable workflow v3 with Development `success|blocked`, independent QA `pass|fail|blocked`, fail-to-remediation, separate Development/QA blocked human gates, Human Review, and Done; v1/v2 remain unchanged.
- 2026-09-08: Added a distinct GPT-5.6 Sol independent-QA controller mode with normal default tools plus additive workflow tools. The global secretless worker now reads the exact bound PR, reviews, discussion, files, commits, check runs, and commit status through native `gh`, publishes one substantive PR report, and has no QA code-publication operation.
- 2026-09-08: Extended the central engine's existing stage capability with exact QA PR-report and report-kind binding. QA pass enters Human Review, fail allocates fresh Development context on the existing PR branch, and blocked enters explicit operator intervention. Cancellation, callback deduplication, row locks, stale/foreign-thread rejection, allowlists, native-auth-only behavior, and one launch/thread/Orb per StageRun remain unchanged.
- 2026-09-08: Added Milestone 7 domain/integration/worker/controller/UI regression coverage and rendered active QA, QA-fail remediation, QA-blocked, and post-pass Human Review states. Visual inspection caught and fixed a clipped QA badge and a misleading green visited marker for failed QA; failed/blocked stages now use red/amber markers.
- 2026-09-08: The first live v3 launch created Development thread `T-01a081b5-cdfa-760b-84da-5eddf6171f06`, which read issue #4, created the authorized incomplete fixture, committed it, and pushed its attempt branch, but ended before opening a PR/reporting/completing. Investigation found the transcript monitor could suppress `agent.end` while simultaneously missing the already-finished response. Removed that suppression, added a regression proving one corrective turn still occurs while monitoring is pending, and versioned the controller registration to v5. The incomplete run and orphan attempt branch remain visible audit evidence rather than being rewritten.
- 2026-09-08: The delayed monitor on the first run eventually continued Development, which opened PR #5 and completed; its QA attempt was cancelled during controller rotation. A second run correctly refused to invent a missing QA finding from that prior PR, exposing the underlying prompt/monitor ordering race: monitoring began after `appendUserMessage`, so a fast response could finish before the subscription. Controller v6 starts `waitForResponse` before appending the prompt, with a regression asserting that ordering. Both runs remain in production history and PR #5 remains unmerged.
- 2026-09-08: Published controller v6 as commit `7689bfa90903c01a228d4db5f6bc9f953fd56a1a`, securely rotated only `AMP_LAUNCH_WEBHOOK_URL`, deployed Laravel Cloud deployment `depl-a2b2b7a0-8de2-40d5-ad3a-bb810af6b8d4`, and cancelled superseded `RUN-0019` through the central engine. Registration remained disabled and the independent operator/repository allowlists remained active.
- 2026-09-08: Created transparent fixture issue `jacovanc/Orc#6` and live-proved Milestone 7 as production `RUN-0020`: Development 1 → substantive QA fail → fresh Development 2 reading the GitHub finding → fresh substantive QA pass → Human Review. Four distinct fresh sandbox threads each had one claimed/delivered/completed launch and one accepted report/completion. PR #7 remains open and unmerged; it changes only `docs/independent-qa-evidence.md`. QA made no code publication and Orc performed no human approval or merge.
- 2026-09-08: Documented the Projects and Amp routing domain before implementation. The installed Plugin API has no project selector on `createThread`; routing therefore uses one verified durable controller connection registered inside each selected Amp project, with run/launch connection snapshots, actual `AMP_PROJECT_ID` evidence, immutable account permission, connection-scoped signing, encrypted secrets, and fail-closed URL validation.
- 2026-09-08: Implemented authenticated personal Projects with canonical GitHub repositories, per-project overview/start/settings/status, all-project run overview, immutable versioned `AmpProjectConnection` records, and safe migration of historical runs/launches into the existing Orc Project without rewriting their attempt or event history.
- 2026-09-08: Replaced runtime email/repository allowlists with explicit `users.can_trigger_amp` authority plus Project ownership and a verified connection. New registrations and historical synthetic users default false; only previously allowlisted operators receive the one-time migration, and profile email changes cannot elevate permission. Connection URLs and directional secrets are encrypted, hidden, SSRF-guarded, and scoped to signed routes; transport failures store only sanitized messages.
- 2026-09-08: Implemented controller contract v7 with exact connection and actual `AMP_PROJECT_ID` validation. Added a harmless fresh-Orb placement/native-`gh` verification path and propagated the child project identity through every stage capability. Normal default agent tools remain additive; no GitHub credential or broad controller secret enters a coding/QA Orb.
- 2026-09-08: Removed the proposed automated human-feedback publication phase. Reviewers now comment directly on the bound GitHub PR and choose Request Changes or Approve in Orc without a URL or confirmation field; Development remediation continues to reread reviews, inline discussion, reports, and CI from GitHub.
- 2026-09-08: Removed the erroneous Human Review feedback-link and accepted-checkbox gate from request validation, controller, engine, and UI. Approval now completes directly; Request Changes idempotently creates one fresh Development attempt bound to the existing PR. Added HTTP and engine regressions for field-free approval/change requests, duplicate submission, and required outcomes.
- 2026-09-08: Live v7 connection verification event `777021b9-acc8-417b-832f-17a9028b618f` was claimed by the correctly bound Orc controller, but Amp did not create a child thread and Laravel remained unverified. Thread-history inspection found no new child. Hardened controller contract v8 so any post-claim fresh-thread/start/prompt failure is reported through the one-time verification capability, safely cancels a partially created thread, and leaves a durable idempotent failed state instead of an indefinitely ambiguous claim.
- 2026-09-08: Live v8 verification confirmed the new fail-closed callback and persisted `controller_thread_failed` with no thread ID or retry. The hosting controller thread already had exactly 20 direct children. Switched v9 verification and stage launches to Amp's documented unparented `createThread` mode so controller child capacity cannot block fresh Orbs; every launched thread remains bound transactionally and must report the exact configured `AMP_PROJECT_ID`.
- 2026-09-08: Live v9 verification events `3781cbca-744d-4a6c-8753-de5947285a37`, `ca9c031c-7240-498f-b735-bf7caf3bcef0`, and delayed retry `98c21486-a7ed-4c4e-8cb9-3236fc78d61d` were each claimed once and then definitively failed before Amp returned a thread ID; the v8 callback recorded each safely and no thread or automatic retry was created. This disproved controller parent capacity as the complete cause and leaves actual project-placement proof blocked on Amp Orb creation in this controller context. Production remains fail-closed with the connection unverified; application-level Project routing is deployed and tested.
- 2026-09-08: Implemented Project setup protocol v1. Project creation now atomically allocates connection identity and directional secrets before webhook registration, plus a 20-minute setup capability. The owner UI supplies a copyable scoped prompt; `orc_setup_project` pins controller source by SHA-256, verifies actual `AMP_PROJECT_ID`, writes only the Orc controller and mode-0600 gitignored runtime, completes pairing over HTTPS after the one Plugin-API-required reload, then automatically queues harmless placement/native-read verification. Reissue revokes prior unused setup and creates a new immutable version without moving existing runs.
- 2026-09-08: The first production setup-prompt acceptance paired connection v5, but its verification delivery remained unclaimed. Investigation confirmed Amp shares durable webhook registrations by plugin key across project threads, so the static v10 key could resolve to an older controller owner. Controller v11 assigns every immutable connection a unique key derived from its public connection ID; re-pairing can no longer inherit a stale handler.
- 2026-09-08: Removed the Amp project ID from Project creation. New Projects now need only a name and canonical GitHub repository; the first authorized setup claim atomically binds the actual `AMP_PROJECT_ID` supplied by the chosen Amp project. Replays, competing project identities, revoked operators, and completion remain fail-closed, while existing Projects and historical run snapshots are unchanged.
- 2026-09-09: Runtime logs identified the remaining verification blocker exactly: one superseded v9 webhook event was retried every 30 seconds with `Webhook handler not found`, and Amp's ordered project-plugin delivery prevented the current v11 event from reaching its handler. Added inert compatibility handlers for retired v9/v10 registrations. They only acknowledge poison deliveries and have no launch, callback, cancellation, or mutation path; current work remains bound to its connection-scoped v11 handler.
- 2026-09-09: Hardened the active v11 handler against ordered-queue poisoning. Permanently invalid, expired, or incorrectly bound input is acknowledged without processing or logging request details; rejected stale/cancelled claims are terminal; transient callback, network, and Orb failures still retry. Added a controller regression proving malformed input cannot call Laravel or create an Orb.
- 2026-09-09: A clean post-drain verification reached the connection-scoped v11 controller and exposed Amp's concrete Orb error: custom agents must be registered as an active agent mode. Registered the harmless connection verifier as a project mode and extended the controller-mode regression; its instructions and sole additive workflow tool remain read-only.
- 2026-09-09: Production Project setup protocol v1 completed end to end. Connection v8 created fresh Orb thread `T-01a0869a-b217-703e-aa61-b767a96e5f9f` in the bound Amp project; its verifier called only `workflow_verify_project_connection`, confirmed native read access to `jacovanc/orc`, changed no files or GitHub/workflow state, and moved the connection to `verified`.
- 2026-09-09: Removed the setup prompt's dependency on the Orc source repository being public. The versioned authoritative runbook is now served without authentication from the deployed Orc domain at `/docs/project-setup-v1`, and generated prompts reference that URL.
- 2026-09-09: Simplified workflow start to require only the issue number. Orc derives and persists the canonical `https://github.com/{project repository}/issues/{number}` URL server-side; the form no longer asks users to copy a link, and unvalidated URL input cannot override the Project binding.
- 2026-09-09: Investigated failed `ai-cms` run 21 / StageRun 33. Its connection had passed fresh-Orb/native-read verification, but the exact stored Amp capability later returned HTTP 404 before a stage thread existed. Added a signed connection-bound webhook refresh on every trusted-controller registration with persistent event deduplication, identity/owner/setup checks, and SSRF validation. Launch 404/410 now also fails the connection explicitly so a retired endpoint cannot remain misleadingly verified. The failed run remains immutable audit history and must not be retried; pair a new connection version, verify it, then start a new run.
- 2026-09-09: Removed the temporary v9/v10 drain registrations after the known poison queue was cleared, so controller reloads no longer recreate obsolete webhooks. The installed Amp Plugin API and CLI expose creation but no supported list/delete/revoke operation; obsolete durable registrations must be removed manually in Amp's UI after checking run bindings.
- 2026-09-09: Reduced the personal worker to three genuine Orc capabilities: project setup, connection verification, and stage completion. Removed dedicated issue/QA readers, GitHub report publishers, and publication recorder. Agents now use normal native `gh`/Git for all GitHub work; `workflow_complete` verifies the native user's exact marker-bound report and open same-repository PR evidence before binding it and completing through Orc.

## Verification

- Current public-runbook / issue-number / webhook-refresh delivery: `php artisan test --compact` **106 passed, 652 assertions**; controller plugin **8 passed, 56 assertions**; Pint, Composer validation, Vite production build, controller bundle, worker bundle, and `git diff --check` passed. `.amp/in/artifacts/workflow-start-issue-number-only.png` was rendered at 2× DPR and inspected: one issue-number field, no issue-URL input, canonical repository visible, no visual defects.
- Tool-surface simplification checks: Laravel **106 passed, 652 assertions**; controller **8 passed, 58 expectations**; personal worker **7 passed, 19 expectations**; controller and worker Node-target bundles, Pint, Composer validation, Vite production build, and both repositories' `git diff --check` passed. Tests assert additive default-tool semantics and exactly three personal Orc tools, including malformed/spoofed GitHub evidence rejection before any Orc mutation.
- `php artisan test --compact`: **42 passed, 148 assertions**.
- `vendor/bin/pint --test`: **68 files passed**.
- `npm run build`: **production assets built successfully**.
- `php artisan migrate:fresh --seed`: **all migrations and Development workflow seed succeeded**.
- Browser screenshots: `.amp/in/artifacts/workflow-agent-active.png`, `.amp/in/artifacts/workflow-human-review.png`, and `.amp/in/artifacts/workflow-completed.png`.
- Laravel Cloud deployment `depl-a2b0d072-0c99-403d-95c8-f7365f9f534f`: **`deployment.succeeded`**; source commit matched `81493c8bf81fc3b1a53f5e65eeb13a767e7863b1`.
- Production `php artisan migrate:status --no-interaction`: **command succeeded with exit code 0** and all four migrations reported `Ran`.
- Production HTTP smoke test: `/`, `/register`, and `/login` returned **200**; unauthenticated `/workflows` returned **302** to `/login`; compiled CSS and JavaScript returned **200**.
- Production browser console/errors were empty. `.amp/in/artifacts/orc-production-home.png` was captured from the deployed application and visually inspected with no layout or styling defects.
- Milestones 4–5 integration tests: **12 passed, 66 assertions**; full suite after integration: **55 passed, 222 assertions**.
- Amp plugin bundle check: `bun build ... --external @ampcode/plugin --target bun` succeeded. `amp plugins list` parsed the plugin and reached the expected managed-Orb-only `createWebhook` guard in standalone CLI mode.
- Rendered integration states passed browser console/error checks and visual inspection: `.amp/in/artifacts/amp-launch-pending.png` and `.amp/in/artifacts/amp-proof-human-review.png`.
- Real production proof links: Development report <https://github.com/jacovanc/Orc/issues/1#issuecomment-5575693984> and QA report <https://github.com/jacovanc/Orc/issues/1#issuecomment-5575697936>. Both associated Amp threads used exactly three workflow tools and completed with the expected outcomes.
- Real authenticated production screenshot `.amp/in/artifacts/amp-production-human-review.png`: visually inspected **PASS**; it shows `RUN-0015`, completed Development/QA attempts with thread and report links, waiting Human Review actions, no visible secrets, and no rendering defects.
- Final Milestone 6 local checks: `php artisan test --compact` **72 passed, 350 assertions**; focused M6 tests **10 passed, 89 assertions**; controller User Plugin tests **4 passed, 18 expectations**; global worker tests **7 passed, 14 expectations**; Pint and Composer validation passed; production Vite build passed; both plugins bundled against external `@ampcode/plugin`; a full fresh migration/seed passed.
- Registration/launch-boundary checks after default-deny hardening: registration tests **4 passed, 17 assertions**; Amp integration and workflow-page tests **24 passed, 138 assertions**; final full suite **73 passed, 359 assertions**; Pint, Composer validation, and `git diff --check` passed.
- Rendered Milestone 6 states passed browser console/error checks and visual inspection: `.amp/in/artifacts/m6-real-development-active.png`, `.amp/in/artifacts/m6-development-blocked.png`, and `.amp/in/artifacts/m6-human-review-proof-warning.png`. The launch identifier shown in the active state is a non-secret stable idempotency identifier.
- Final live-acceptance checks: Laravel **73 passed, 359 assertions**; controller **4 passed, 20 expectations**; global worker **7 passed, 14 expectations**; Pint, Composer validation, Vite production build, both plugin bundles, and `git diff --check` passed. `.amp/in/artifacts/m6-live-acceptance-human-review.png` passed browser console/page-error checks and visual inspection with the proof-only warning, distinct thread references, PR #3/branch links, Human Review actions, no visible secrets, and no rendering defects.
- Post-ownership hardening checks: Laravel **74 passed, 370 assertions**, including a regression proving a removed operator cannot use request-changes to create an attempt, launch, or queue job and that the Human Review attempt remains active; Pint, Composer validation, and `git diff --check` passed.
- Laravel Cloud deployment `depl-a2b0ead3-da4a-4a12-9e4d-b82fb1a5c500`: **`deployment.succeeded`**, source commit `ff5732952ed511ffa2fced9d4fa00a7e008782cd`.
- Final Laravel Cloud deployment `depl-a2b11f89-b89b-47f7-91f1-8d35091be5a0`: **`deployment.succeeded`**, source commit `daa5b06aef4b56e2f37c9b54375b09d984bf2e72` containing the live-proven controller topology and production screenshot.
- Milestone 6 Laravel Cloud deployment `depl-a2b14b1c-1278-4fe9-95ac-6f22147860b9`: **`deployment.succeeded`**, source commit `bc81554e34056adf00f28a013d49350930dec751`. Production command `comm-a2b14bf8-9e38-4c70-805f-acf58265ae60` exited 0 and reported all seven migrations `Ran`, including `2026_09_07_230000_add_real_development_workflow` in batch 4.
- Final Milestone 6 handoff deployment `depl-a2b14cb3-da65-4bef-a26d-da07b18f925a`: **`deployment.succeeded`**, source commit `38ebbac7c1a98b400aeebb663e3a00967043681f` containing the complete tracked deployment record.
- Registration hardening deployment `depl-a2b15035-56e7-4247-a55c-56c4f1f6418c`: **`deployment.succeeded`**, source commit `801f09f364deb5f250c667fbf0750cf053095398`. Live Cloud configuration checks confirmed registration disabled, Amp integration enabled, and non-empty independent operator/repository allowlists without exposing their contents. Both `GET /register` and a valid-CSRF `POST /register` returned **404**; `/`, `/login`, and `/up` remained **200**.
- Final v3 controller deployment `depl-a2b2548c-a553-4a4b-bdad-d9d0566262a3`: **`deployment.succeeded`**, source commit `388dbccafd85a49cf3953009213c0bf247bd5889`. Production matched the newly loaded owner-only webhook capability without exposing it; registration remained disabled, integration and both independent allowlists remained active/non-empty, `/register` returned **404**, and `RUN-0017` remained waiting at Human Review. Persistent events showed one claimed launch per agent attempt plus one safely deduplicated Development reconciliation (`duplicate_claim`/`already_launched`), with no extra Orb, report, or completion.
- Operator ownership hardening deployment `depl-a2b282cc-a8b1-48d3-a345-965fe60983a3`: **`deployment.succeeded`**, source commit `b7195cc9b1b068d594780d5439547b3d2efe85ef`. Production verification confirmed the designated account owns `RUN-0017`, is explicitly allowlisted, and has exactly one append-only reassignment event; registration remains disabled. Prior allowlist entries were preserved.
- Post-deployment HTTP smoke: `/`, `/login`, and `/up` returned **200**; `/register` returned **404**; unauthenticated `/workflows` returned **302** to `/login`; the compiled CSS asset returned **200**.
- Pre-deployment Milestone 7 checks: Laravel **82 passed, 484 assertions**; focused Milestone 7 **8 passed, 114 assertions**; controller **4 passed, 27 expectations**; global worker **9 passed, 25 expectations**; Pint, Composer validation, Vite production build, fresh migration/seed, both plugin bundles, and `git diff --check` passed.
- Rendered Milestone 7 states passed browser console/page-error checks and final visual inspection: `.amp/in/artifacts/m7-running-qa.png`, `.amp/in/artifacts/m7-qa-fail-remediation.png`, `.amp/in/artifacts/m7-qa-blocked.png`, and `.amp/in/artifacts/m7-human-review.png`.
- Post-race-fix checks: Laravel **82 passed, 484 assertions**; focused workflow tests **28 passed, 143 assertions**; controller **5 passed, 31 expectations** including monitor-before-prompt ordering; global worker **9 passed, 25 expectations**; Pint, Composer validation, Vite production build, both plugin bundles, and `git diff --check` passed. Final inspection of `.amp/in/artifacts/m7-human-review.png` passed with truthful substantive-QA and separate-human-decision messaging, legible evidence links/actions, no visible secrets, and no rendering defects.
- Live `RUN-0020` persistence audit: four `AmpLaunch` rows were each claimed, delivered in one attempt, thread-bound, and completed. Append-only integration events contained one claim, acknowledgement, report claim, report attestation, and completion per agent attempt (plus publication for each Development attempt); workflow events recorded four launches, two publications, four reports, four completions, and the waiting Human Review stage.
- Laravel Cloud background process `process-a2b0eb69-ac71-4ef1-806e-93a70c442d92`: one database worker on queue `amp-launches`, four tries, bounded backoff, and a 30-second worker timeout.
- Projects/routing pre-deployment checks: Laravel **91 passed, 516 assertions**; controller **5 passed, 36 expectations**; global worker **10 passed, 29 expectations**; both plugin bundles, Composer validation, Pint, Vite production build, fresh migration/seed, and `git diff --check` passed. Coverage includes two-project concurrent routing, immutable connection snapshots, owner isolation, profile-email non-escalation, connection-scoped callbacks, cross-connection launch rejection, encrypted secrets, SSRF rejection, and idempotent fresh-Orb verification.
- Rendered Projects UI passed browser console/page-error checks. `.amp/in/artifacts/projects-overview.png`, `.amp/in/artifacts/project-detail.png`, and `.amp/in/artifacts/project-connection-settings.png` were inspected; a cramped count label and low-contrast project metadata were corrected, and the settings capture exposed no webhook or secret value.
- Projects/routing post-hardening checks: Laravel **93 passed, 529 assertions**; controller **6 passed, 39 expectations**; focused routing **11 passed, 46 assertions**; Pint, Composer validation, Vite production build, controller bundle, and `git diff --check` passed. The new regression proves a claimed connection thread-creation failure is durably and idempotently reported without retrying an Orb.
- Human Review/setup checks: Laravel **100 passed, 603 assertions**; controller **6 passed, 41 expectations**; global worker **11 passed, 35 expectations**; Pint, Composer validation, Vite production build, both plugin bundles, fresh migration/seed, and `git diff --check` passed. Coverage includes no-field approval/change requests, duplicate human actions, setup allocation/claim/completion, same-thread idempotency, wrong-project/thread rejection, expiry/revocation/reissue, operator permission revocation, encrypted capability storage, no token reflection, and truthful setup states. `.amp/in/artifacts/human-review-no-feedback-gate.png` and `.amp/in/artifacts/project-setup-prompt.png` were inspected and passed; the latter deliberately redacts its narrow setup capability.
- Final no-ID setup hardening checks: Laravel **102 passed, 620 assertions**; controller **7 passed, 47 expectations**; Pint and Composer validation passed; the controller bundle compiled against external `@ampcode/plugin`; `git diff --check` passed. Laravel Cloud deployments `depl-a2b456e1-c98e-415e-83c7-0fdd44fef98c` and `depl-a2b492f5-c8c7-404e-bb38-e2b053f62a86` succeeded from commits `2b66d239ea67f5453ae359f11d0fe99b2b4ad37d` and `7ec844219529daffdbbdda973d702ec656c8f6b5`; post-deploy `/`, `/login`, and `/up` returned **200**, while disabled `/register` returned **404**.

## Deployment

Production is live at <https://orc-production-trttyo.laravel.cloud/>.

- Source: public GitHub repository `jacovanc/Orc`, branch `main`.
- Laravel Cloud application: `app-a2b0cf1b-4c0d-4cde-9370-d7f30f4516d0` in `eu-west-2`.
- Environment: `env-a2b0cf1c-fca2-46f1-992a-19399332cb4c`, PHP 8.4, Node.js 22, push-to-deploy enabled.
- Database: private Laravel MySQL 8.4 cluster `orc-production`, 512 MB flexible compute, 5 GB storage, scheduled snapshots with two-day retention, and a 60-second idle suspend interval.
- Environment variables include a Laravel Cloud-managed `APP_KEY`, production mode with debug disabled, the canonical application URL, and stderr logging. Secret values were never printed or committed.
- Build uses optimized production Composer dependencies and compiled Vite assets. Deploy runs `php artisan migrate --force && php artisan db:seed --force`.
- Integration variables and independent directional secrets are configured without exposing their values. `AMP_INTEGRATION_ENABLED` is active; the deployed database queue worker delivers to the durable controller webhook.
- Production includes connection-scoped controller v11 behavior from commit `7ec844219529daffdbbdda973d702ec656c8f6b5`. Registration is disabled, the designated existing operator retains explicit immutable launch permission, synthetic/new accounts are denied by default, and the current Orc Project connection is verified through a fresh Orb with native repository access.
- The global User Plugin and project controller are loaded. The controller exposes the additive proof, Development, independent QA, and read-only connection-verifier modes; neither plugin provisions or contains GitHub credentials.
